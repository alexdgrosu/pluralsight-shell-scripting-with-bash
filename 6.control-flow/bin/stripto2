#!/bin/bash

if [[ ! $1 ]]; then
    echo "Need first argument" >&2
    exit 1
fi

found=
while read -r || [[ $REPLY ]]; do
    if [[ ! $found ]]; then
        if [[ $REPLY =~ $1 ]]; then
            found="true"
        else
            continue
        fi
    fi
    echo "$REPLY"
done

exit 0